<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EJS Tags</title>
</head>
<body>
    <h1><%=title%></h1>
    <h2><%=seconds%> in to the game.</h2>
    <% if (seconds % 2 === 0) { %>
    <ul>
        <% for ( i = 0; i < items.length; i++) { %>
        <li><%= items[i]; %></li>
        <% } %>
    </ul>
    <% } %>
    <%- htmlContent %>

    <%- include("footer.ejs") %>

</body>
</html>